# Known Bugs — Cloudskraal CapEx Backend

No bugs identified at this time.

- No `TODO`, `FIXME`, `HACK`, or `XXX` markers in `src/`
- No "fix" commits in git history for `backend/`
- No tests exist, so latent issues may be present but unverified

## Reporting template

When a bug is found, add an entry below using this format:

```
### <short title>
- **Where:** src/path/to/file.js:LINE
- **Symptom:** what the user / caller sees
- **Repro:** minimal steps or request
- **Severity:** low / medium / high
- **Notes:** workaround, suspected cause, related commit
```
