# Known Bugs

No open bugs identified from source scan (no `TODO`/`FIXME`/`HACK` markers in `src/`).

Historical:

- TypeScript strict-mode build errors on Vercel — fixed in `c0a4d43` and `4360752`. Watch for regressions when enabling new strict flags or upgrading `typescript` / `typescript-eslint`.

## Reporting

Add entries below as:

```
## <short title>
- Route / component:
- Repro:
- Expected:
- Actual:
- Notes:
```
